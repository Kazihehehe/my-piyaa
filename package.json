{
  "name": "project",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "browserslist": ["defaults"],
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Client-side assets built'",
    "serve": "serve -s . -l 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "body-parser": "^2.2.0",
    "express": "^5.1.0",
    "serve": "^14.2.1"
  }
}
